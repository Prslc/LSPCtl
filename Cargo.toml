[package]
name = "LSPCtl"
version = "0.1.2"
edition = "2024"

[dependencies]
clap = { version = "4.5.54", features = ["derive"] }
rusqlite = { version = "0.37.0", features = ["bundled"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
